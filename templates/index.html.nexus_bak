{% extends 'base.html' %}

{% block breadcrumbs %}
<span class="text-secondary">Nexus</span>
<svg class="w-4 h-4 text-label/60" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
<span class="text-primary">Dashboard</span>
{% endblock %}

{% block content %}
<div id="dashboardWrapper" class="w-full h-full flex flex-col space-y-8 animate-fade-in p-6 overflow-hidden">
    <!-- Quick Stats -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <div class="kpi-card-premium">
            <div class="kpi-card-bar bg-primary"></div>
            <div class="flex items-center gap-10">
                <div class="flex-shrink-0">
                    <div class="kpi-icon-wrapper bg-primary/10 text-primary">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path></svg>
                    </div>
                </div>
                <div class="flex flex-col">
                    <span class="text-sm font-bold text-label/60 tracking-tight uppercase">Total Eventos</span>
                    <h3 class="text-3xl font-black text-body-text tracking-tighter mt-1" id="kpiTotalTasks">--</h3>
                </div>
            </div>
        </div>


        <!-- Card 2: Tareas Pendientes (Peach) -->
        <div class="kpi-card-premium">
            <div class="kpi-card-bar bg-peach"></div>
            <div class="flex items-center gap-10">
                <div class="flex-shrink-0">
                    <div class="kpi-icon-wrapper bg-peach/10 text-peach">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    </div>
                </div>
                <div class="flex flex-col">
                    <span class="text-sm font-bold text-label/60 tracking-tight uppercase">Alertas Críticas</span>
                    <h3 class="text-3xl font-black text-body-text tracking-tighter mt-1" id="kpiPendingTasks">--</h3>
                </div>
            </div>
        </div>


        <!-- Card 3: Tareas Programadas (Violet) -->
        <div class="kpi-card-premium">
            <div class="kpi-card-bar bg-violet"></div>
            <div class="flex items-center gap-10">
                <div class="flex-shrink-0">
                    <div class="kpi-icon-wrapper bg-violet/10 text-violet">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                    </div>
                </div>
                <div class="flex flex-col">
                    <span class="text-sm font-bold text-label/60 tracking-tight uppercase">Sesiones Activas</span>
                    <h3 class="text-3xl font-black text-body-text tracking-tighter mt-1" id="kpiScheduledTasks">--</h3>
                </div>
            </div>
        </div>


        <!-- Card 4: Tarea Activa (Emerald) -->
        <div class="kpi-card-premium">
            <div class="kpi-card-bar bg-emerald-500"></div>
            <div class="flex items-center gap-10">
                <div class="flex-shrink-0">
                    <div class="kpi-icon-wrapper bg-emerald-500/10 text-emerald-500">
                        <svg class="w-8 h-8 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                    </div>
                </div>
                <div class="flex flex-col">
                    <span class="text-sm font-bold text-label/60 tracking-tight uppercase">Estado Seguridad</span>
                    <h3 class="text-3xl font-black text-body-text tracking-tighter mt-1" id="kpiActiveTask">ÓPTIMO</h3>
                </div>
            </div>
        </div>

    </div>

    <!-- Main Grid Content -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 flex-grow min-h-0 items-stretch">
        <!-- Dashboard Quad-Core (Left Section) -->
        <div class="lg:col-span-2 grid grid-cols-1 gap-8">
            <div id="cardVolumen" class="kpi-card-premium group min-h-[220px]">
                <!-- Operative Content Grid -->
                <div id="volumeContent" class="grid grid-cols-[1fr_4fr] h-full items-center relative z-10 gap-8 p-4">
                    <div class="grid grid-rows-[auto_1fr] h-full">
                        <div class="kpi-icon-wrapper bg-sky/10 text-sky shadow-inner">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                        </div>
                        <div class="flex flex-col justify-end mt-8">
                            <h3 class="text-4xl font-black text-bi-main tracking-tighter" id="kpiOperationalVolume">--</h3>
                            <p class="text-xs font-black text-sky uppercase tracking-[0.2em] mt-2">Volumen Operativo</p>
                            <p class="text-[12px] text-label/40 font-bold mt-1">Registros procesados</p>
                        </div>
                    </div>
                    <div class="relative w-full h-52 flex items-center justify-center">
                        <div id="bookingSparkline" class="w-full h-full"></div>
                    </div>
                </div>

                <!-- Premium No-Data State (Spans Full Width) -->
                <div id="noDataVolume" class="hidden absolute inset-0 z-20 flex items-center justify-center p-8 animate-in fade-in zoom-in duration-700">
                    <div class="flex items-center gap-10 max-w-xl">
                        <div class="w-24 h-24 rounded-[40px] bg-sky/5 border-2 border-sky/10 flex items-center justify-center text-sky/20 relative group shrink-0">
                            <div class="absolute inset-0 bg-sky/10 blur-2xl rounded-full opacity-30 group-hover:opacity-60 transition-all"></div>
                            <svg class="w-12 h-12 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
                        </div>
                        <div class="text-left">
                            <h4 class="text-lg font-black text-bi-main uppercase tracking-[0.3em] mb-2 italic">Flujo de Telemetría Cerrado</h4>
                            <p class="text-xs text-label/40 font-bold uppercase tracking-widest leading-relaxed">El ecosistema operativo se encuentra en estado de reposo.<br>No se detectan transferencias de registros en el periodo actual.</p>
                            <div class="flex items-center gap-2 mt-4">
                                <div class="w-2 h-2 rounded-full bg-sky/40 animate-pulse"></div>
                                <span class="text-[10px] font-black text-sky/40 uppercase tracking-widest">Sincronizando con el nodo...</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div id="cardCumplimiento" class="kpi-card-premium group min-h-[220px]">
                <!-- History Content Grid -->
                <div id="historyContent" class="grid grid-cols-[1fr_4fr] h-full items-center relative z-10 gap-8 p-4">
                    <div class="grid grid-rows-[auto_1fr] h-full">
                        <div class="kpi-icon-wrapper bg-primary/10 text-primary shadow-inner">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                        </div>
                        <div class="flex flex-col justify-end mt-8">
                            <h3 class="text-4xl font-black text-bi-main tracking-tighter" id="kpiEfficiency">--</h3>
                            <p class="text-xs font-black text-primary uppercase tracking-[0.2em] mt-2">Detalle de tareas</p>
                            <p class="text-[12px] text-label/40 font-bold mt-1">Desempeño cronológico reciente</p>
                        </div>
                    </div>
                    <div class="relative w-full h-52 flex items-center justify-center">
                        <div id="onTimeRadial" class="w-full h-full"></div>
                    </div>
                </div>

                <!-- Premium No-Data State (Spans Full Width) -->
                <div id="noDataHistory" class="hidden absolute inset-0 z-20 flex items-center justify-center p-8 animate-in fade-in zoom-in duration-700">
                    <div class="flex items-center gap-10 max-w-xl">
                        <div class="w-24 h-24 rounded-[40px] bg-primary/5 border-2 border-primary/10 flex items-center justify-center text-primary/20 relative group shrink-0">
                            <div class="absolute inset-0 bg-primary/10 blur-2xl rounded-full opacity-30 group-hover:opacity-60 transition-all"></div>
                            <svg class="w-12 h-12 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                        </div>
                        <div class="text-left">
                            <h4 class="text-lg font-black text-bi-main uppercase tracking-[0.3em] mb-2 italic">Historial de Analítica Vacío</h4>
                            <p class="text-xs text-label/40 font-bold uppercase tracking-widest leading-relaxed">No se han registrado eventos significativos en el historial.<br>La base de datos de auditoría está lista para indexar.</p>

                            <div class="flex items-center gap-2 mt-4">
                                <div class="w-2 h-2 rounded-full bg-primary/40 animate-pulse"></div>
                                <span class="text-[10px] font-black text-primary/40 uppercase tracking-widest">Esperando señal de tareas...</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Activity Monitor (Right Column) -->
        <div class="lg:col-span-1 flex flex-col bg-panel-fill border border-panel-border shadow-2xl shadow-primary/5 rounded-2xl pt-6 px-0 pb-0 relative overflow-hidden group h-full">
            <div class="absolute -right-12 -top-12 w-48 h-48 bg-primary/5 blur-3xl rounded-full group-hover:bg-primary/10 transition-all"></div>
            
            <!-- Header -->
            <div class="flex items-center justify-between pb-4 mb-0 px-8 border-b border-primary/10 dark:border-primary/30 relative z-10 transition-colors">
                <div class="flex items-center gap-4">
                    <div class="w-2 h-2 rounded-full bg-success animate-pulse"></div>
                    <h2 class="text-[11px] font-black text-label/80 uppercase tracking-[0.3em]">Live Activity</h2>
                </div>
                <!-- Audit Link Button -->
                <a href="{{ url_for('audit.index') }}" class="group/btn w-8 h-8 flex items-center justify-center rounded-xl bg-surface-container/20 hover:bg-primary/10 border border-panel-border hover:border-primary/30 transition-all active:scale-95 shadow-sm" title="Ver Historial Completo">
                    <svg class="w-3.5 h-3.5 text-primary transition-transform group-hover/btn:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
                </a>
            </div>
            
            <!-- Body: Activity (Shared Premium Component) -->
            <div class="flex-grow overflow-hidden custom-scrollbar bg-surface/20 relative z-10" id="activityLogsContainer">
                {% if activity %}
                    <div class="flex flex-col divide-y divide-panel-border/20 animate-in fade-in slide-in-from-right-4 duration-700">
                        {% for log in activity %}
                            {% set action_lower = log.action | lower %}
                            {% set icon = 'fas fa-fingerprint' %}
                            {% set color_class = 'text-primary' %}
                            {% set bg_class = 'bg-primary/10' %}
                            {% set border_class = 'border-primary/20' %}

                            {% if 'operación' in action_lower or 'ejecución' in action_lower or 'tarea' in action_lower %}
                                {% set icon = 'fas fa-bolt' %}
                                {% if 'éxito' in action_lower or 'terminada' in action_lower or 'completada' in action_lower %}{% set color_class = 'text-success' %}{% set bg_class = 'bg-success/10' %}{% set border_class = 'border-success/20' %}
                                {% elif 'error' in action_lower or 'abort' in action_lower %}{% set icon = 'fas fa-exclamation-triangle' %}{% set color_class = 'text-peach' %}{% set bg_class = 'bg-peach/10' %}{% set border_class = 'border-peach/20' %}
                                {% else %}{% set color_class = 'text-sky' %}{% set bg_class = 'bg-sky/10' %}{% set border_class = 'border-sky/20' %}
                                {% endif %}
                            {% elif 'acceso' in action_lower or 'login' in action_lower or 'logout' in action_lower %}
                                {% set icon = 'fas fa-shield-alt' %}{% set color_class = 'text-violet' %}{% set bg_class = 'bg-violet/10' %}{% set border_class = 'border-violet/20' %}
                            {% elif 'modificación' in action_lower or 'creación' in action_lower or 'modific' in action_lower or 'crea' in action_lower %}
                                {% set icon = 'fas fa-edit' %}{% set color_class = 'text-indigo-400' %}{% set bg_class = 'bg-indigo-400/10' %}{% set border_class = 'border-indigo-400/20' %}
                            {% endif %}

                            <div class="px-4 py-2 hover:bg-white/[0.02] transition-all duration-500 group relative overflow-hidden">
                                <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/[0.01] to-transparent opacity-0 group-hover:opacity-100 transition-opacity pointer-events-none"></div>
                                <div class="flex items-start gap-3 relative z-10">
                                    <div class="w-9 h-9 rounded-xl {{ bg_class }} border {{ border_class }} flex items-center justify-center flex-shrink-0 transition-transform group-hover:scale-110 duration-500 shadow-inner">
                                        <i class="{{ icon }} {{ color_class }} text-[13px]"></i>
                                    </div>
                                    <div class="flex-grow min-w-0 flex flex-col gap-1">
                                        <div class="flex items-center justify-between gap-2">
                                            <span class="{{ color_class }} text-[10px] font-black uppercase tracking-widest leading-none truncate">
                                                {{ log.action }}
                                            </span>
                                            <span class="text-[9px] font-black text-label/90 uppercase tracking-tighter tabular-nums">
                                                {{ log.timestamp.strftime('%H:%M') }}
                                            </span>
                                        </div>
                                        <p class="text-[10px] font-bold text-body-text/80 truncate leading-snug group-hover:text-body-text transition-colors px-1" title="{{ log.detail }}">
                                            {{ log.detail if log.detail else 'Sin detalles' }}
                                        </p>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <div class="h-full flex flex-col items-center justify-center p-10 opacity-30">
                        <div class="w-16 h-16 rounded-full border-2 border-dashed border-label/20 flex items-center justify-center mb-6">
                            <i class="fas fa-satellite-dish text-2xl animate-pulse"></i>
                        </div>
                        <span class="text-[10px] font-black uppercase tracking-[0.3em] text-center italic">Escaneando Actividad...</span>
                    </div>
                {% endif %}
            </div>

            <!-- Footer Pagination (Shared High Fidelity) -->
            <div class="border-t border-panel-border bg-panel-header/20 p-3 flex items-center justify-between z-10 backdrop-blur-sm" id="activityPaginationFooter">
                <span class="text-[9px] font-black text-label/40 uppercase tracking-tighter" id="paginationInfo">Pág. {{ pagination.page if pagination else 1 }} de {{ pagination.pages if pagination else 1 }}</span>
                <div class="flex items-center gap-1.5">
                    {% if pagination and pagination.has_prev %}
                        <button onclick="changeActivityPage({{ pagination.prev_num }})" class="w-7 h-7 flex items-center justify-center rounded-lg bg-surface-container border border-panel-border text-label/60 hover:text-primary transition-all active:scale-95">
                            <i class="fas fa-chevron-left text-[10px]"></i>
                        </button>
                    {% else %}
                        <div class="w-7 h-7 flex items-center justify-center rounded-lg bg-surface-container/50 border border-panel-border/30 opacity-30">
                            <i class="fas fa-chevron-left text-[10px]"></i>
                        </div>
                    {% endif %}

                    {% if pagination and pagination.has_next %}
                        <button onclick="changeActivityPage({{ pagination.next_num }})" class="w-7 h-7 flex items-center justify-center rounded-lg bg-surface-container border border-panel-border text-label/60 hover:text-primary transition-all active:scale-95">
                            <i class="fas fa-chevron-right text-[10px]"></i>
                        </button>
                    {% else %}
                        <div class="w-7 h-7 flex items-center justify-center rounded-lg bg-surface-container/50 border border-panel-border/30 opacity-30">
                            <i class="fas fa-chevron-right text-[10px]"></i>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
  </div>

<!-- Dashboard Logic Assets -->
<script src="{{ url_for('core.serve_assets', filename='js/dashboard.js') }}"></script>
<script src="{{ url_for('core.serve_assets', filename='js/dashboard_1.js') }}"></script>
{% endblock %}
